# Server-only
GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile
GROQ_FALLBACK_MODEL=llama-3.1-8b-instant

# Firebase Admin SDK (server-only; do not use NEXT_PUBLIC_ variables)
# In Vercel, paste the private key with literal \n characters between lines.
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=""
